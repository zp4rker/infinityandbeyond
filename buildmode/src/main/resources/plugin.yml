main: com.zp4rker.iab.buildmode.BuildMode
api-version: ${mcVersion}
depend:
  - Bukkot
  - InfinityAndBeyond

name: ${pluginName}
prefix: ${pluginPrefix}
version: ${version}
description: ${description}